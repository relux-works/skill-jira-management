# TASK-260212-2ojx1f: define-credentials-model

## Description
Define credentials data model: instance URL, email, API token. Create struct with validation rules (URL format, email format, non-empty token). Document field purposes and constraints.

## Scope
(define task scope)

## Acceptance Criteria
- Credentials struct defined (instance URL, email, token)
- Validation rules implemented (URL format, email format, non-empty token)
- Field constraints documented
- Model supports future extensibility
