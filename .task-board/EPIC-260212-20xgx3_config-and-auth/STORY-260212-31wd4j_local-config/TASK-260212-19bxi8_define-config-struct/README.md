# TASK-260212-19bxi8: define-config-struct

## Description
Define config struct with fields: active project, active board, locale (ru/en). Define sensible defaults (locale from system, empty active project/board). Document config schema and field purposes.

## Scope
(define task scope)

## Acceptance Criteria
- Config struct defined with active project, active board, locale fields
- Sensible defaults defined (locale from system, empty project/board)
- Config schema documented
- Struct supports future extensibility
