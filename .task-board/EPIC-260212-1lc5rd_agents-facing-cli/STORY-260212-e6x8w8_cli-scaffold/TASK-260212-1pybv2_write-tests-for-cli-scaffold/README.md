# TASK-260212-1pybv2: Write tests for CLI scaffold

## Description
Unit tests for: root command initialization, global flag parsing (--project, --board, --format), version command output. Use Go testing framework.

## Scope
(define task scope)

## Acceptance Criteria
- Tests verify root command initializes correctly
- Tests verify all global flags parse correctly
- Tests verify version command output
- Tests verify flag validation (e.g., invalid --format)
- All tests pass with 'go test'
